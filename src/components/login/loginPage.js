// import components
import ComponentsLogin from "./login"
import Logo from "../utility/logo";

const LoginPage = () => {
  return (
    <div className="login">
        <Logo />
        <ComponentsLogin />
    </div>
  );
};

export default LoginPage;
